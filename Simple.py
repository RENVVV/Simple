from rich import print as cetak
import os
if __name__ == "__main__":
   try:
       os.system("git pull")
   except Exception as e:
       exit(str(e))
cetak(f"[bold green]Sorry, we closed the simple script, thank you in advance for those who have used this script, we have maximized so that this script is useful, because many users say that this script has a bad script ")
